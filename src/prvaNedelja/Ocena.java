package prvaNedelja;
/// enum (skraćeno od enumeration/nabrajanje) je poseban tip podataka koji predstavlja fiksni skup konstantnih vrednosti.
/// Koristi se kada znaš da neka promenljiva može imati samo određene dozvoljene vrednosti.
public enum Ocena {
    PET,
    SEST,
    SEDAM,
    OSAM,
    DEVET,
    DESET
}
